<nav>
  <ul>
    <li>
      <a
        href="/"
        <% if (locals.PageTitle=="HOME") { %> class="active" <% } %>
      >Home</a>
    </li>

    <li>
      <a
        href="/settings"
        <% if (locals.PageTitle=="SETTINGS") { %> class="active" <% } %>
      >Home</a>
    </li>
  </ul>
</nav>